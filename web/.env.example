# Leave empty in local dev so Vite proxies /api to localhost:8080
# In production set to your Cloud Run URL, e.g. https://mam-medal-api-xxxxx.run.app
VITE_API_BASE_URL=

# Set to "true" only when Square production keys are live (changes button to Buy now)
VITE_SQUARE_LIVE=true
