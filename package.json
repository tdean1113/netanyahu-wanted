{
  "name": "netanyahu-wanted-medal",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:api": "npm --prefix api run dev",
    "build:web": "npm --prefix web run build",
    "build:api": "npm --prefix api run build"
  }
}
